{
  "name": "robinAI",
  "version": "1.0.0",
  "description": "open a terminal, go to the directory and run node deploy-commands.js to sync up the commands\r then run node index.js to run the bot\r run nohup node index.js, on a linux. Windows equivalents aint great, but you can use the start cmd",
  "main": "index.js",
  "dependencies": {
    "@discordjs/rest": "^0.4.1",
    "discord-api-types": "^0.33.0",
    "discord.js": "^13.7.0",
    "dotenv": "^16.0.1"
  },
  "devDependencies": {
    "@types/node": "^17.0.34"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC"
}
