open a terminal, go to the directory and run node deploy-commands.js to sync up the commands
then run node index.js to run the bot
run forever start node index.js &, on a linux. Windows equivalents aint great, but you can use the start cmd